import { Fragment } from "react"

const LoginForm = (props) => {
    return (
        <Fragment>
            <Card>
                
            </Card>
        </Fragment>
    )
}

export default LoginForm