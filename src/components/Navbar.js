import { Fragment } from "react"

const Navbar = (props) => {
    const [navbarContents,setnavbarContents]=use
    return (
        <Fragment>
            
        </Fragment>
    )
}

export default Navbar