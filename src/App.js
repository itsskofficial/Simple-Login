import Navbar from "./components/Navbar";

const App = () => {
  return (
    <frage
    <Navbar loginSuccessful={onLoginHandler} onLogout={onLogoutHandler}>

    </Navbar>

  )
}

export default App;
